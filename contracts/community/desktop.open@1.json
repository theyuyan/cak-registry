{
  "name": "desktop.open",
  "version": "1.0.0",
  "description": "在用户屏幕上用系统默认程序打开工作区内文件或 http(s) 网址（用户能看见窗口：macOS open / Linux xdg-open / Windows start）——用户说\"打开给我看\"\"用浏览器打开\"就是它，不是 browser.open（那是 agent 自己用的无头浏览器，用户看不到）。target 只能是工作区内的文件路径（相对 CAK_WORKSPACE，符号链接按真实目标判，越界拒）或 http(s) 网址；file:/javascript: 等其他 scheme 一律拒绝。app 仅 macOS 有效（open -a 指定应用）。副作用 external：默认需要审批。设 DESKTOP_DRY_RUN=1 时只校验不真开（method=dry-run）。",
  "inputSchema": {
    "type": "object",
    "required": [
      "target"
    ],
    "additionalProperties": false,
    "properties": {
      "target": {
        "type": "string",
        "minLength": 1,
        "maxLength": 4096
      },
      "app": {
        "type": "string",
        "minLength": 1,
        "maxLength": 200
      }
    }
  },
  "outputSchema": {
    "type": "object",
    "required": [
      "ok",
      "platform",
      "target",
      "method"
    ],
    "additionalProperties": false,
    "properties": {
      "ok": {
        "type": "boolean"
      },
      "platform": {
        "enum": [
          "darwin",
          "linux",
          "win32"
        ]
      },
      "target": {
        "type": "string"
      },
      "method": {
        "type": "string"
      }
    }
  },
  "permissions": [
    "fs.read"
  ],
  "sideEffects": "external",
  "idempotent": false,
  "defaultTimeoutMs": 15000,
  "async": false,
  "schemaDigest": "sha256:616b6a3f490ba2160f8d4c58093cea8c396175ca58b931fd1644fe2211d68d9c"
}
